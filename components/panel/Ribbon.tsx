/** Cinta del contrato visual, con texto honesto de fase 1 (principles.md). */
export function Ribbon() {
  return (
    <div className="ribbon">
      Fase 1 · datos sembrados · sin llamadas a API externa
    </div>
  );
}
