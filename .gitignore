# Dependencias
node_modules/
/.pnp
.pnp.js

# Next.js
/.next/
/out/
/build
/dist

# Entorno — datos de negocio / secretos: NUNCA commitear (solo .env.example)
.env
.env.local
.env*.local

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Supabase (artefactos locales)
.branches
.temp
supabase/.temp

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# Datos reales / PII — NUNCA al repo público (GDPR). El mockup lleva nombres
# de prospectos reales; se conserva solo local. Los datos reales van a Supabase.
panel-operativo-mockup.html
*.real.local.*
# Seed de PRODUCCIÓN con clientes reales: se carga a tu Supabase, nunca al repo.
supabase/seed.prod.local.sql
*.prod.local.*

# SO / editor
.DS_Store
*.pem
.idea/
.vscode/

# Flujo generado (maquetas + borradores de correo de prospectos reales) — nunca al repo.
salida/
